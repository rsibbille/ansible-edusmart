---
- name: Durcir les accès SSH des serveurs Linux EduSmart
  hosts: "{{ target_hosts | default('ansible_controllers:linux_servers') }}"
  become: true
  gather_facts: false

  roles:
    - role: hardening_ssh
