---
- name: Configurer les DNS internes EduSmart
  hosts: ansible_controllers:idm_servers:mail_servers:smtp_relays:teleport_servers
  become: true
  gather_facts: true

  roles:
    - role: dns_client
