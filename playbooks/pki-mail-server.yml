---
- name: Générer le certificat TLS du serveur mail EduSmart
  hosts: mail_servers
  serial: 1
  become: true
  gather_facts: false

  vars_files:
    - ../inventories/production/vault/pki.yml

  roles:
    - role: pki_ldap_server
