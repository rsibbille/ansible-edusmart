---
docker_container_restart_policy: "no"
docker_container_restart_policy_inspect: "no:0"

docker_startup_script_path: /usr/local/sbin/start-edusmart-containers
docker_startup_service_name: edusmart-containers.service

docker_startup_wait_timeout: 300
docker_startup_inter_group_delay: 10

docker_startup_required_containers:
  - grafana-postgres
  - passbolt-db
  - nginx-proxy-manager
  - node-exporter
  - cadvisor
  - prometheus
  - alertmanager
  - grafana
  - passbolt
  - ldap-account-manager

docker_startup_groups:
  - label: "Étape 1 : bases de données"
    containers:
      - grafana-postgres
      - passbolt-db

  - label: "Étape 2 : proxy et exporters"
    containers:
      - nginx-proxy-manager
      - node-exporter
      - cadvisor

  - label: "Étape 3 : supervision"
    containers:
      - prometheus
      - alertmanager

  - label: "Étape 4 : applications"
    containers:
      - grafana
      - passbolt
      - ldap-account-manager

# Fichiers Compose dont la politique doit être persistée sur l'hôte.
docker_startup_unmanaged_compose_files:
  - /opt/edusmart/stacks/monitoring/compose.yml
  - /opt/edusmart/stacks/passbolt/compose.yml
  - /opt/edusmart/stacks/ldap-account-manager/compose.yml
  - /opt/edusmart/stacks/nginx-proxy-manager/compose.yml
